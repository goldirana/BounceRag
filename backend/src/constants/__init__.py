from pathlib import Path

CONFIG_FILE_PATH = str(Path("config.yaml"))
PARAMS_FILE_PATH = str(Path("params.yaml"))
PROMPT_FILE_PATH = str(Path("backend/src/prompts/summarizer.json"))
FIREBASE_CREDENTIALS_PATH = str(Path("credentials/firebase_credentials.json"))